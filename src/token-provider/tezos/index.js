import factory from "./factory";
import providers from "./providers";

export default {
    // Convenience factory method to build all providers with default options.
    factory,
    // Map of provider classes namespaced by Provider.key
    providers,
};
